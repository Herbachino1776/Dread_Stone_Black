# Imported NPC weapons

`IMPORT_WEAPON.cmd` writes one validated JSON record here and copies its GLB to
`public/assets/weapons/imported/`. Vite discovers these records for Creature Lab
and the production NPC loadout registry.
