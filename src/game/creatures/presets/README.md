# Authored enemy presets

Creature Lab's **Save Preset to Project** action writes validated Enemy Preset
JSON records here. The production preset registry discovers them automatically,
which also makes them available to Encounter Authoring Mode.
